# Optional: enables OpenAI narrative generation when --narrative is used.
# Keep real values in an ignored local .env file or your shell environment.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_BASE_URL=https://api.openai.com/v1
